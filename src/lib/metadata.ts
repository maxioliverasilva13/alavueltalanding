/** Re-export SEO helpers (metadata + JSON-LD). */
export {
  alavueltaIcons,
  loadingMetadata,
  empresaMetadata,
  buildLocalBusinessJsonLd,
  landingPublicOrigin,
  landingPublicUrl,
} from "./seo";
